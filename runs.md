# Run log

Convention: one section per run with config hash, tokens, wall-clock, tok/s,
and eval numbers (ppl / IFEval-lite / grounding / MT-Bench-lite coherence +
judge mean). Add blind-chat notes ("would I keep chatting?").

No runs yet — first up: `mllm-150m` pretrain to probe the floor.
